[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'gunicorn app:app --bind 0.0.0.0:${PORT:-8080} --workers 2 --timeout 120'"
healthcheckPath = "/"
restartPolicyType = "on_failure"

[environments.production.variables]
FLASK_ENV = "production"
